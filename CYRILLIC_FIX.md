# 🔧 Исправление проблемы с кириллицей в PDF

## Что было сделано:

### 1. ✅ Обновлена кодировка в layout.blade.php
- Добавлен `lang="ru"` на HTML тег
- Добавлена правильная кодировка UTF-8:
  - `<meta charset="UTF-8">`
  - `<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">`

### 2. ✅ Изменен шрифт
- Теперь: `font-family: DejaVu Sans, serif;`
- DejaVu Sans — стандартный шрифт DOMPDF, отлично поддерживает кириллицу

### 3. ✅ Очищен кэш шрифтов
- Удалены файлы: `storage/fonts/*`
- При следующем запуске DOMPDF пересоздаст кэш с правильным шрифтом

### 4. ✅ Каждая таблица на новой странице
- Все таблицы изолированы в `report-section` с `page-break`
- Таблица №1.1 → Новая страница
- Таблица №1.2 → Новая страница
- Таблица №1.3 → Новая страница
- Таблица №1.4 → Новая страница

---

## 🚀 Если проблема с символами сохранилась:

### Вариант 1: Очистите весь кэш
```bash
php artisan cache:clear
php artisan config:cache
rm -rf storage/fonts/*
```

### Вариант 2: Установите дополнительный шрифт (если помогает)
```bash
# Добавьте в конфиг dompdf.php:
'default_font' => 'DejaVu Sans',
```

### Вариант 3: Проверьте конфиг DOMPDF
Файл: `config/dompdf.php`

Убедитесь, что есть эти опции:
```php
'default_paper_size' => 'a4',
'default_paper_orientation' => 'portrait',
'enable_html5_parser' => true,
```

---

## 📝 Что нужно помнить о кириллице в DOMPDF:

✅ **Работает хорошо:**
- DejaVu Sans (все символы)
- DejaVu Serif (все символы)
- Arial Unicode (все символы)

❌ **Проблемы с:**
- Произвольные веб-шрифты (не все шрифты поддерживают кириллицу)
- Шрифты без встроенного подмножества (subset)

---

## 🔍 Проверка кодировки в файле:

Если вы видите в браузере правильно, но в PDF неправильно — это проблема конвертации.

**Проверьте:**
1. Blade шаблон использует UTF-8: ✅ (установлено в header)
2. БД возвращает UTF-8 текст: Проверьте коллейшн DB
3. DOMPDF получает UTF-8: ✅ (передано правильно)

---

## 📞 Если ничего не помогает:

1. **Попробуйте другой бэкенд:**
   ```php
   // В config/dompdf.php
   'pdf_backend' => 'CPDF', // или 'PDFLib'
   ```

2. **Пересяздитесь на другой пакет:**
   ```bash
   composer require mpdf/mpdf
   ```

3. **Проверьте версию DOMPDF:**
   ```bash
   composer show | grep dompdf
   ```

---

## Файлы которые были обновлены:

1. ✅ `resources/views/pdf/layout.blade.php` — кодировка и шрифт
2. ✅ `resources/views/pdf/ru/sections/basic-metrics.blade.php` — таблицы на новых страницах
3. ✅ `storage/fonts/` — кэш очищен

---

После этих изменений кириллица должна отображаться правильно, и каждая таблица будет на новой странице! 🎉
